.panel-footer(ng-if="(footer.pages.max > 1) || !$.isEmptyObject(footer.bulk.options)")
  .row
    div.col-md-4
      with-selected(
        options="footer.bulk.options"
      )
    pagination.col-md-8.text-right(
      pages="footer.pages"
    )
      
    
