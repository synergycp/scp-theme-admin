extends /resources/angle/pug/edit

block title
  span(
    translate='group.form.title.EDIT'
    translate-value-name='{{vm.edit.input.name}}'
  )

block form
  group-form(form="vm.edit")

block append right-side
  group-buttons(group="vm.edit.input")
