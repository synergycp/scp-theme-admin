extends /resources/angle/pug/edit

block title
  span(
    translate='system.integration.form.title.EDIT'
    translate-value-name='{{vm.edit.input.name}}'
  )

block form
  integration-form(form="vm.edit")  

block append right-side
  integration-buttons(integration="vm.edit.input")
