list(list="tab.list")
  .panel-body
    .btn-group.btn-group-xs.pull-right
      a.btn.btn-default(ng-click="tab.filter.visible = !tab.filter.visible")
        i.fa.fa-filter
  entity-filters(
    show="tab.filter.visible"
    current="tab.filter.current"
    change="tab.filter.change()"
    should-watch-main-search="true"
  )
  entity-table.border-top-table.scrollable-block
  list-footer
