extends /resources/angle/pug/edit

block title
  span(
    translate='pxe.preseed.form.title.EDIT'
    translate-value-name='{{vm.edit.input.name}}'
  )

block form
  preseed-form(form="vm.edit")

block append right-side
  preseed-buttons(preseed="vm.edit.input")
