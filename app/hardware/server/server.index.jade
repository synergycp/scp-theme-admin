extends /resources/jade/list-page
include /resources/jade/mixins/list

block list
  include server.list.jade

block bottom-left
  +create-form("{{ 'hardware.server.form.title.ADD' | translate }}")
    server-form(form="vm.create")

block bottom-right
  logs(
    title="Server Logs"
    filter="vm.logs.filter"
    listen-to="vm.list"
  )
