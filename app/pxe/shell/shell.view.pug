extends /resources/angle/pug/edit

block title
  span(
    translate='pxe.shell.form.title.EDIT'
    translate-value-name='{{vm.edit.input.name}}'
  )

block form
  shell-form(form="vm.edit")  

block append right-side
  shell-buttons(shell="vm.edit.input")
