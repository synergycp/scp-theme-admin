doctype html
include /resources/angle/pug/mixins/form

+field(
  "{{ 'speed.form.name.LABEL' | translate }}",
  "name"
)(
  placeholder="{{ 'speed.form.name.PLACEHOLDER' | translate }}"
  ng-model="speedForm.input.name"
)

+field(
  "{{ 'speed.form.billing_id.LABEL' | translate }}",
  "billing_id"
)(
  placeholder="{{ 'speed.form.billing_id.PLACEHOLDER' | translate }}"
  ng-model="speedForm.input.billing_id"
)

+field(
  "{{ 'speed.form.speed.LABEL' | translate }}",
  "speed"
)(
  placeholder="{{ 'speed.form.speed.PLACEHOLDER' | translate }}"
  ng-model="speedForm.input.speed"
)
