extends /resources/jade/list-page
include /resources/jade/mixins/list

block list
  include preseed.list.jade

block bottom-left
  +create-form("{{ 'pxe.preseed.form.title.ADD' | translate }}")
    preseed-form(form="vm.create")

block bottom-right
  logs(
    title="PXE Preseed Logs"
    filter="vm.logs.filter"
    listen-to="vm.list"
  )
