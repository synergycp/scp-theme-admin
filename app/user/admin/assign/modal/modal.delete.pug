include /resources/angle/pug/mixins/form

form(ng-submit="modal.submit()")
  .modal-header
    button.close(
      type="button"
      ng-click="modal.$dismiss()"
    ) &times;

    h4.modal-title(translate="admin.modal.delete.TITLE")

  .modal-body
    p(translate="admin.modal.delete.BODY")

    ul
      li(
        ng-repeat="admin in modal.admins"
        ng-bind="admin.name"
      )

  .modal-footer
    button.btn.btn-secondary(
      type="button"
      ng-click="modal.$dismiss()"
      data-translate="admin.modal.delete.CANCEL"
    )
    button.btn.btn-danger(
      type="submit"
      data-translate="admin.modal.delete.SUBMIT"
    )
