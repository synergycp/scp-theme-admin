extends /resources/angle/pug/table-checkable

block headings
  th Name
  th Actions

block columns
  td
    a(
      ui-sref="app.pxe.boot.view({id: row.id})"
      ng-bind="row.name"
    )
  td
    .btn-group.btn-group-xs
      a.btn.btn-default(
        ui-sref="app.pxe.boot.view({id: row.id})"
        translate="app.action.EDIT"
      )
