extends /resources/angle/pug/edit

block title
  span(
    translate='forward.gateway.form.title.EDIT'
    translate-value-name='{{vm.edit.input.name}}'
  )

block form
  forward-gateway-form(form="vm.edit")

block append right-side
  forward-gateway-buttons(gateway="vm.edit.input")
