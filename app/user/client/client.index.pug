extends /resources/pug/list-page
include /resources/pug/mixins/list

block list
  include client.list.pug

block bottom-left
  +create-form("{{ 'user.client.form.title.ADD' | translate }}")
    client-form(form="vm.create")

block bottom-right
  logs(
    title="Client Logs"
    filter="vm.logs.filter"
  )
